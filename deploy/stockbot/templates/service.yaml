apiVersion: v1
kind: Service
metadata:
  name: {{ include "stockbot.fullname" . }}
  labels:
    {{- include "stockbot.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "stockbot.selectorLabels" . | nindent 4 }}
